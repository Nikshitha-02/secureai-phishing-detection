import { LandingPage } from '@/pages/LandingPage';

/**
 * App root.
 * Routing will be added in a future task (React Router).
 * For now, render the landing page directly.
 */
function App() {
  return <LandingPage />;
}

export default App;
